[HIGH]
- roll back on non-zero exit status/error
- package python-crontab
- fix error about lack of beautifulsoup when running tests
- fix error with releases_count metrics
- add a custom graph for releases_count
- fix error with tablesorter due to dragtable
- add 1-2 more metrics
- make table columns deletable
- add multiple metrics to table
- display common portion when graphing multiple metrics
- add pagination to table
- make display names for columns different from column names for db

[LOW]
- historical data injection
- customizable page templates
- stable URLs for combined metrics

[DONE]
- add git hook to email upon commit
- use Shell=False
- use ini as filename extension for config
- change URL to DB_URI in config file
- use ConfigParser for config file parsing
- make directory name for manifests configurable
- create a sample pull script using CSV
- create another sample pull script using CSV
- create script to generate graphs
- pull data from database when generating graphs
- display some graphs on the website
- automatic time series plotting
- create crontab
- implement python-crontab for crontab parsing
- provide a python api for graphing
- move pull scripts and graph script to their own directory
- add graphs for releases and releases_count
- add more to config file format
- display releases and releases_count graphs on website
- add override_ts and graph_type to manifest
- add table to graph_runner.py
- merge graph_runner.py into pull_runner.py
- add metric from irc bot for number of packages vs time
- add graph_type key to manifest (pie, line, histogram, etc.)
- implement scaffolding to run tests
- add table as default method of display of data
- use astring.title() in table2class function
- add python-iter to requirements
- add docstrings to pull_runner_test
- use self.assertEqual in tests
- add additional pull_runner tests
- finish the switch to SQLAlchemy in pull_runner
- rename filename used by the table
- display tables on website
- import models variable in create_all.py
- change the tables to use HTML instead of matplotlib
- generalize the flask code
- make makefile use directory in config file
- add JQuery plugin to make sticky headers
- add Jquery plugin to sort columns
- decide how to authenticate push metrics
- add website page to display graphs for irc bot
- pull out common runner code into runner_helper.py
- add push runner
- add push route to web interface
- only run pull_runner for pull metrics
- update tests for runner changes
- use absolute paths
- add push_runner to the crontab file
- use autofmt_xdate() when graphing
- remove day locator from xaxis for readability
- use "with open()" in runner_helper
- use a variable dir in makefile instead of hardcoded
- rename rc_bug to rc_bug_count
- create manifest2index.py and add it to makefile
- add description of metric to manifest file
- get initial dynamic web interface working
- use css for tabs on static interface instead of JS
- generate push graph upon data being pushed
- move non-python files to root dir
- add table to dynamic web interface
- import debmetrics.css on index page
- clearfix for collapsing container on graph tab
- fix flot-graph-legend style
- use layout.html as template for index page
- make JQuery plugin sort the date column correctly
- fix attempt relative import in non-package error
- move target all to top of Makefile
- generate csv for download by user
- remove tabbed interface to put graph and table on same page
- use debian.css
- fix error with releases metric
- Fix path to pull_runner.py in crontab
- Move create_all.py to root dir
- Don't quote strings
- Swap metric list and add metric button
- Tablesorter css override so that up/down arrows don't overlap header text
- Allow graph dimensions to be changed
- Add thanks.html
- Add jquery.dragtable.js and use it
- Move download csv link
- Make options accordion dropdown
- Allow multiple metrics to be added to the graph
- Make columns show/hide when metric name is clicked
- Resize graph instead of readding metric
- Prevent resizing of graph to 0 width or height
- Add remove metric button
- Add exit(0) to pull_runner.py
- Add delete buttons to legend
- Only add metric to graph if it was not already added
- Improve delete button
- Insert all rows into db instead of just last row
- Add delete_on_insert to manifests
- Add grab handle to table
- Display metric name in csv download link
- Add xaxis update option
- Correct path to graphs dir in graph_helper.py
- Add Session.commit() to db_delete_all()
- Populate x-axis options with default values
- HTML validator fixes
- rework landing interface and URLs
- stable URLs for individual metrics
- default to static interface
- remove tabbed interface from static interface
- download csv link for static interface
- Set SQLAlchemy timeout to 90 seconds
- Add dpkg_formats metric
- Add json parser and tab-delimited parser
- add more parsers other than csv
- switch to python3
- do all operations in unicode
